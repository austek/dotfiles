#!/bin/zsh

if [[ -z "$1" ]]; then
  echo "Usage: gradle_prop <property_name>" >&2
  exit 1
fi

awk -v key="$1" '
    index($0, key "=") == 1 {
        sub(/^[^=]*=/, "")
        print
        exit
    }
' ~/.gradle/gradle.properties
